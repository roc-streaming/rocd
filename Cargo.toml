[package]
name = "rocd"
version = "0.1.0"
edition = "2024"

[dependencies]
salvo = { version = "0.77", features = ["logging"] }
tokio = { version = "1", features = ["macros"] }
tracing = { version = "0.1" }
tracing-subscriber = { version = "0.3" }
